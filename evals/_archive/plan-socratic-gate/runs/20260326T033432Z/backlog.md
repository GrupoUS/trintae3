# Still to improve

## Knowledge Gaps

- Binary criteria cap reached at 15/15 after iter 1.
- The "fast path" (user says "just plan it") behavior could be a dedicated TC — not tested against a skimming agent explicitly.
- Live agent grading would test whether "costly re-planning" is concrete enough to change real behavior.

## Next Actions

- Future candidate: add a concrete example of what a re-planning incident looks like (e.g., "architect chose Option A, found out in Phase 3 that X was also affected").
- Consider TC: "User asks for a refactor with no explicit scope — does agent ask the 3 questions or proceed?"

## Plateau

Reached at iteration 1 — max score 15/15 with current criterion set.
