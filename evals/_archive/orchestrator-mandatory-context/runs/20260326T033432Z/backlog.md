# Still to improve

## Knowledge Gaps

- Binary criteria cap reached at 15/15 after iter 1.
- The downstream `> MANDATORY CONTEXT RULE` callout and Critical Rule 9 already cover C4 — the issue was C5 (at-point-of-copy visibility). Confirmed consistent with delegate.md fix.
- Live grading needed to verify agents actually fill all 5 fields vs leaving `[placeholder]` text.

## Next Actions

- `evals/delegate/runs/20260326T030818Z/backlog.md` noted: "Do NOT redo is the most commonly skipped field in practice — consider adding a filled-in example." That backlog item applies equally here to the orchestrator MANDATORY CONTEXT block.
- Future candidate: add one filled-in example of a complete MANDATORY CONTEXT block using a real Grupo US task (e.g., "add new hero CTA" task context).

## Plateau

Reached at iteration 1 — consistent with the delegate.md run (same fix, same pattern, same plateau).
