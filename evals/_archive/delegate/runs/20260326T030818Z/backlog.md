# Still to improve

## Knowledge Gaps

- Binary criteria cap reached at 15/15 after iter 1. No further binary improvement without expanding criterion set.
- Format hints (e.g., "Name: 1-2 sentences" for Prior findings) would help in practice but don't change binary pass/fail scoring.
- Field ordering (plan state before prior findings) was marginal tie — not promoted.

## Next Actions

- Consider a second eval cycle with live delegation test cases to catch real-world gaps that binary scoring misses.
- "Do NOT redo" is the most commonly skipped field in practice — consider adding a filled-in example to the command.
- Future candidate: add a 1-line example showing a bad delegation (no context) vs good delegation (full context).

## Plateau

Reached at iteration 1 — max score 15/15 with no further binary improvements available in the current criterion set.
